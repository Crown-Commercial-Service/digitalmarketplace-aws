---
api:
  subdomain: dm-api
  services:
    - digitalmarketplace_api_db

search-api:
  subdomain: dm-search-api

admin-frontend:
  subdomain: dm
  path: /admin

buyer-frontend:
  subdomain: dm

supplier-frontend:
  subdomain: dm
  path: /suppliers

briefs-frontend:
  subdomain: dm
  path: /buyers

db-snapshot:
  subdomain: dm-db-snapshot
  services:
    - digitalmarketplace_api_db
